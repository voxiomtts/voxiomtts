# ==== CORE (pinned exact versions for stability) ====
torch==2.1.0
torchaudio==2.1.0
customtkinter==5.2.1
sounddevice==0.4.6
soundfile==0.12.1
pydub==0.25.1
simpleaudio==1.0.4
noto-emoji-fonts==2022-03-23  # Font files as Python package

# ==== COMPATIBILITY (flexible but bounded) ====
tqdm>=4.0.0,<5.0.0          # Avoid major breaking changes
requests>=2.0.0,<3.0.0
omegaconf>=2.1.1,<3.0.0
numpy>=1.21.0,<2.0.0        # Torch compatibility
matplotlib>=3.5.0,<4.0.0

# ==== CRITICAL ADDITIONS ====
librosa==0.10.0             # For audio processing safety
protobuf==3.20.3            # Often needed by torch/torchaudio
typing_extensions==4.5.0    # For Python<3.11 compatibility

# ===== DEVELOPMENT =====
black==23.7.0                  # Code formatting
flake8==6.0.0                  # Linting
pytest==7.4.0                  # Testing
ipython==8.14.0                # Debugging REPL

# ===== PLATFORM-SPECIFIC =====
# Windows: pip install pyaudio (may need pre-built wheel)
# Linux: sudo apt-get install python3-pyaudio ffmpeg
# Mac: brew install portaudio ffmpeg
